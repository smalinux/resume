

```bash
# Templates
#git clone https://github.com/dnl-blkv/mcdowell-cv
#git clone https://github.com/deedy/Deedy-Resume

# install tex
sudo apt-get install texlive-full
sudo apt install texstudio

# install Times New Roman font
sudo apt install ttf-mscorefonts-installer
# Then rebuild the font cache
sudo fc-cache -fv

# RUN (LuaTeX) !!! important !!!
# In TeXstudio: Options > Configure TeXstudio > Build > set compiler to XeLaTeX or LuaLaTeX
texstudio &
```

### Ref
- My template: https://www.careercup.com/resume
- Tex editor: https://www.texstudio.org/
